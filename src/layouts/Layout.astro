---
const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title} - Nicopel</title>
    <!-- Chart.js para gráficos rápidos -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script> 
</head>
<body class="bg-slate-50 font-sans text-slate-800">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar -->
        <aside class="w-64 bg-slate-900 text-white flex flex-col shadow-xl">
            <div class="p-6 text-xl font-bold tracking-wider border-b border-slate-700">
                NICOPEL <span class="text-blue-400">ERP</span>
            </div>
            <nav class="flex-1 p-4 space-y-2">
                <a href="/" class="block px-4 py-3 rounded hover:bg-slate-800 transition flex items-center gap-3">
                    📊 Dashboard
                </a>
                <a href="/compras/nova" class="block px-4 py-3 rounded hover:bg-slate-800 transition flex items-center gap-3">
                    🛒 Nova Compra
                </a>
                <a href="/estoque" class="block px-4 py-3 rounded hover:bg-slate-800 transition flex items-center gap-3">
                    📦 Estoque
                </a>
                <a href="#" class="block px-4 py-3 rounded hover:bg-slate-800 transition flex items-center gap-3 opacity-50 cursor-not-allowed">
                    💰 Financeiro (Breve)
                </a>
            </nav>
            <div class="p-4 border-t border-slate-700 text-xs text-slate-400">
                v1.0.0
            </div>
        </aside>

        <!-- Main Content -->
        <main class="flex-1 overflow-y-auto p-8">
            <slot />
        </main>
    </div>
</body>
</html>